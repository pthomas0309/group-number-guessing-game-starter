// will need access to the newGuesses array (create it's own file?)
module.exports = () => {
    const newGuessesArray = require("./modules/guesses.js"); // empty newGuessesArray
    const randomNumber = require(".module/randomNumber.js"); // randomNumber function


    for (let something of newGuessesArray){
        let winner = "",
        if (something.pal == randomNumber){
            winner
        }
    }
}
// will ne